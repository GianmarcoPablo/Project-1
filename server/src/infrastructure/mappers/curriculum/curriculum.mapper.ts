import { CustomError } from "../../../domain/errors";
import { CurriculumEntity } from "../../../domain/entities";

export class CurriculumMapper {
    static curriculumEntityFromObject(object: { [key: string]: any }) {

    }
}